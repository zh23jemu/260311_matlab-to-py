from __future__ import annotations

"""项目范围内的固定约定。

本文件只保存“跨模块共享且基本不会在运行时变化”的常量，
例如故障编号顺序、需要删除的列、特殊测试样本数规则等。

把这些内容集中放在一起，主要是为了避免：
1. 同一个规则散落在多个文件里，后续修改时漏改。
2. 读代码时不知道某个数字是业务约定还是临时写死的魔法值。
"""

# 参与分类器训练与评估的故障编号顺序。
# 这个顺序会直接影响：
# 1. 分类标签 1~17 的映射关系
# 2. 训练特征拼接顺序
# 3. 混淆矩阵的标签顺序
TRAIN_FAULT_IDS = [1, 2, 4, 5, 6, 7, 8, 10, 11, 12, 13, 14, 16, 17, 18, 19, 20]

# DAE 训练阶段会使用 d00~d20 的全部工况，因此这里是完整编号范围。
ALL_IDS = list(range(21))

# 原始数据是 52 维，按文档要求删除第 46 列和第 50 列。
# Python 使用 0-based 索引，因此对应 45 和 49。
DROP_COLS = [45, 49]


def test_slice_count(fault_id: int) -> int:
    """返回某个故障在测试阶段实际采用的样本数。

    这里不是简单地“把所有测试数据全部用掉”，而是刻意对齐 MATLAB 版本中的
    评估习惯：
    - F6 只取 247 个样本
    - 其他参与评估的故障统一取 2000 个样本

    这样做是为了让 Python 结果与原始流程的口径保持一致。
    """
    return 247 if fault_id == 6 else 2000
